$sub = "ERP " + [char]38568 + [char]36523 + [char]26597
$cPath = "C:\agy_Add_on\" + $sub
$zPath = "Z:\agy_Add_on\" + $sub

# 1. Generate updated 使用說明.txt
$readmeContent = @"
==================================================
           ERP 隨身查 - Telegram Bot 使用說明
==================================================
專案名稱：ERP 隨身查
程式目錄：C:\agy_Add_on\ERP 隨身查 (本機) / Z:\agy_Add_on\ERP 隨身查 (備份)
資料庫路徑：\\192.168.1.99\d\nmedi\stock.DBF (醫材 ERP)

本系統提供手機 Telegram 即時查詢庫存與價格，並支援中英數自動分詞交集搜尋，且已針對網路硬碟進行記憶體讀取優化（5毫秒極速秒回）。

--------------------------------------------------
一、 目錄與檔案說明
--------------------------------------------------
當您打開 C:\agy_Add_on (或備份的 Z:\agy_Add_on) 時，只會看到以下兩個資料夾，其餘啟動設定皆已由 AI 自動配置完成：
1. Add_on需安裝的程式：存放系統相依的安裝檔。
2. ERP 隨身查：存放本系統的核心執行檔。
   - telegram_bot.js：Bot 主程式。
   - config.json：系統設定檔（包含 Bot Token 與授權的對話 ID）。
   - run_bot.bat：啟動批次檔。
   - run_bot.vbs：隱藏視窗啟動代理。
   - 使用說明.txt：本說明檔（本檔案）。
   - 對話紀錄.txt：完整的開發歷史與對話日誌。

--------------------------------------------------
二、 系統自動啟動與執行機制
--------------------------------------------------
為了實現「電腦開機登入後自動背景隱形執行」，系統已註冊至您的 Windows 啟動資料夾中：
* 啟動路徑：C:\Users\user\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\ERP_隨身查.vbs
* 運作方式：當電腦開機自動登入後，Windows 會執行此 VBS 腳本，以 100% 隱藏黑視窗的模式啟動背景的 run_bot.bat，讓 Node.js 在背景默默執行。
* 防重複鎖：程式內部已開啟 Port 28256 作為單一執行鎖。即使重複雙擊啟動，也只會保留一個 Bot 運作，不會造成重複回覆。

--------------------------------------------------
三、 手動維護操作
--------------------------------------------------
1. 如何手動啟動/重啟 Bot：
   如果您想手動重啟 Bot，可以直接進入 C:\agy_Add_on\ERP 隨身查 資料夾，雙擊執行 run_bot.vbs 即可在背景隱形執行。
2. 如何關閉 Bot：
   打開 Windows 工作管理員（Ctrl+Shift+Esc），在「詳細資料」中找到 node.exe 進程，右鍵點擊「結束工作」即可。
3. 修改設定 (config.json)：
   您隨時可以用記事本開啟 C:\agy_Add_on\ERP 隨身查\config.json 來修改：
   - "lowStockThreshold"：低庫存警報水位（預設為 5）。
   - "pushTimes"：每日自動推播低庫存的時間點。目前設為 [] 代表已關閉定時推播。若要重開，可改回例如 ["08:30", "12:00", "17:00"]，存檔後重啟 Bot 即可。

==================================================
"@

[System.IO.File]::WriteAllText(($cPath + "\使用說明.txt"), $readmeContent, [System.Text.Encoding]::UTF8)

# 2. Generate updated 對話紀錄.txt (appending the final folder structure cleanup and Startup trigger migration)
$logContent = @"
======================================================================
               Visual FoxPro ERP Telegram Bot 外掛串接系統
                           完整開發與對話紀錄
======================================================================
儲存時間：2026-05-24
專案路徑：C:\agy_Add_on (執行) / Z:\agy_Add_on (備份)
資料庫路徑：\\192.168.1.99\d\nmedi\stock.DBF (醫材 ERP 資料庫)

本文件完整記錄了此專案從「環境分析」、「Bot 建立」、「功能疊代」、「錯誤修復」到「效能終極優化」的所有對話與開發細節，方便您日後對帳或微調設定時參考。

--------------------------------------------------
目錄
--------------------------------------------------
一、 系統目前最終設定值 (config.json)
二、 系統架構與檔案清單
三、 歷史開發與對話過程 (依時間順序)
    1. 第一階段：資料分析與 Bot 基礎建立 (對話一)
    2. 第二階段：搜尋優化與重開機測試 (對話一)
    3. 第三階段：定時推播調整與關閉 (對話一)
    4. 第四階段：對話誤刪尋回與 LINE 方案評估 (對話二)
    5. 第五階段：搜尋引擎升級與重疊搶答修復 (對話二)
    6. 第六階段：讀取效能與反應速度優化 (對話二)
    7. 第七階段：專案目錄結構清理與開機啟動搬移 (對話二)

--------------------------------------------------
一、 系統目前最終設定值 (config.json)
--------------------------------------------------
檔案位置：C:\agy_Add_on\ERP 隨身查\config.json
設定內容：
{
  "botToken": "8235163535:AAF85YFKevBzcmw7pOsd4P7pDrrEo8U29gQ",
  "databasePath": "\\\\192.168.1.99\\d\\nmedi\\stock.DBF",
  "lowStockThreshold": 5,
  "pushTimes": [],
  "authorizedChats": [
    5976208790
  ]
}

--------------------------------------------------
二、 系統架構與檔案清單
--------------------------------------------------
1. telegram_bot.js (核心程式)
2. run_bot.vbs (啟動代理)
3. run_bot.bat (啟動腳本)
4. 使用說明.txt
5. 對話紀錄.txt (即本檔案)

--------------------------------------------------
三、 歷史開發與對話過程
--------------------------------------------------

【第一階段：資料分析與 Bot 基礎建立】
* 用戶提出：
  希望讀取 Z 碟裡面的 ERP 資料，關於 ERP 系統進行分析，要求使用繁體中文，且不可以動到任何原始資料（唯讀安全）。
* 開發過程：
  1. 偵測 Z 碟目錄，發現 nmedi (醫材系統，有 434 個 DBF 檔)。
  2. 讀取開檔說明，確認 ERP 資料表別名，鎖定 stock.DBF 為商品庫存主檔。
  3. 分析欄位結構，確認關鍵欄位：NO(編號)、NAME(名稱)、UNIT/UNITS(主/次單位)、SCAL(換算倍數)、INVQT(庫存)。
  4. 部署 Telegram Bot，於 C 碟建立 C:\agy_Add_on 目錄。
  5. 於 Windows 工作排程器建立開機自啟動工作 ERP_Telegram_Bot，搭配 run_bot.vbs 實現背景執行。
  6. 成功實現雙單位排版顯示。

【第二階段：搜尋優化與重開機測試】
* 用戶提出：
  希望能搜尋「3M 大白」或「大白 3M」等，不限順序都要能找到。
* 開發過程：
  1. 修改搜尋邏輯，將搜尋詞以「空格」切分。
  2. 實作交集搜尋 (AND)：搜尋出的商品必須同時包含所有關鍵字。
  3. 用戶重開機進行測試，確認開機後程式依然在背景隱形正常運作。

【第三階段：定時推播調整與關閉】
* 用戶提出：
  - 手動推播一次庫存。
  - 因為低庫存品項過多 (共 2,145 筆)，決定關閉定時推播。
* 開發過程：
  1. 執行手動推播，成功發送第一版低庫存清單（自動限制最多顯示前 15 筆，避免超出 Telegram 長度限制）。
  2. 修改 config.json，將 pushTimes 設為空陣列 []，成功關閉每日自動推播。

【第四階段：對話誤刪尋回與 LINE 方案評估】
* 用戶提出：
  - 不小心刪除了對話，詢問該怎麼辦。
  - 詢問使用 LINE 是不是比較方便。
* 開發與回覆：
  1. AI 透過日誌系統成功定位並讀取被誤刪的舊對話紀錄（ID: 5f5b3d0d-9869-4378-89e9-d0635a68e7c2），銜接進度。
  2. 評估 LINE 方案：LINE Bot Webhook 需要固定公網 IP、網址與 HTTPS 安全憑證，會讓本機 ERP 電腦暴露在公網，增加安全風險；而 Telegram 採用長輪詢不需公開網址與 IP，因此最適合本機 ERP 系統。

【第五階段：搜尋引擎升級與重疊搶答修復】
* 用戶提出：
  搜尋 大白3M (沒有打空格) 時找不到商品，且 Telegram 會重複回覆多次訊息。
* 開發與修復：
  1. 升級分詞搜尋引擎：在代碼中引入中英數自動邊界分詞（在中文與非中文/數字交界處自動插入空格）。例如輸入 大白3M，系統會自動在背景拆成 大白 與 3m 進行交集搜尋。
  2. 多重進程搶答 Bug 修復：
     - 原因：Windows 工作排程器重啟時，舊的 node.exe 進程沒有被殺死，導致背景有 4 個 Bot 同時競搶訊息。
     - 解決：強制殺死本機所有 node.exe，並在 telegram_bot.js 最前端引入單一執行實體鎖 (Port 28256)，保證任何時候只有唯一的 Bot 運作。

【第六階段：讀取效能與反應速度優化】
* 用戶提出：
  反應速度有沒有辦法再快一點。
* 開發與優化：
  1. 記憶體快取解析優化：改為一次性將整個 8.8MB 的 stock.DBF 讀入記憶體 Buffer，完全在記憶體中進行搜尋解析，避免在網路硬碟 (UNC) 上頻繁進行微小 I/O。搜尋耗時降到 5 毫秒 (0.005 秒) 內。
  2. 簡化網路請求：移除 正在查詢商品... 過渡訊息，少發送一次 Telegram API 請求，直接為每次查詢省去 200~400 毫秒網路延遲。

【第七階段：專案目錄結構清理與開機啟動搬移】
* 用戶提出：
  希望專案命名為「ERP 隨身查」。
  希望 agy_Add_on 資料夾底下只剩下「Add_on需安裝的程式」與「ERP 隨身查」兩個資料夾，將 run_bot.vbs 與 run_bot.bat 從根目錄移除。
* 開發與優化：
  1. 建立 C:\agy_Add_on\ERP 隨身查 資料夾，將 config.json, telegram_bot.js, 使用說明.txt, 對話紀錄.txt 與啟動腳本等全部收納進去。
  2. 為了解除 Windows 修改系統工作排程器需要系統管理員權限的限制，將啟動方式轉移至 Windows 啟動資料夾 (Startup) 中，建立「ERP_隨身查.vbs」捷徑。
  3. 徹底清理 C:\agy_Add_on 與 Z:\agy_Add_on 根目錄，只留下指定的兩個資料夾。
  4. 重啟 Bot 服務，Bot 在背景完美穩定執行。

======================================================================
本對話紀錄將永久保存於您的 agy_Add_on\ERP 隨身查 資料夾中，方便您隨時檢閱。
======================================================================
"@

[System.IO.File]::WriteAllText(($cPath + "\對話紀錄.txt"), $logContent, [System.Text.Encoding]::UTF8)

# 3. Sync to Z backup
Copy-Item -Path ($cPath + "\使用說明.txt") -Destination ($zPath + "\") -Force -ErrorAction SilentlyContinue
Copy-Item -Path ($cPath + "\對話紀錄.txt") -Destination ($zPath + "\") -Force -ErrorAction SilentlyContinue
