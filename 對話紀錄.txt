======================================================================
         Visual FoxPro ERP Telegram Bot 外掛串接與行動盤點系統
                            完整開發與對話紀錄
======================================================================
儲存時間：2026-05-27
專案路徑：C:\agy_Add_on\ERP_System (執行，無 Z 槽備份以防系統商窺探)
資料庫路徑：\\192.168.1.99\d\nmedi\stock.DBF (醫材 ERP 資料庫)

本文件完整記錄了此專案從「環境分析」」、「Bot 建立」、「、「盤點點助」、「開機器人設定」到「安全性防範」的所有對話與開發細節，方便您日後參考。

----------------------------------------------------------------------

----------------------------------------------------------------------
一、 系統目前最終設定值 (config.json)
二、 系統架構與檔案清單
三、 歷史開發與對話過程 (依時間順序)
    1. 第一階段：資料分析與 Bot 基礎建立 (對話一) (對話一)
    2. 第二階段：搜尋優化與重開機測試 (對話一)(對話一)
    3. 第三階段：定時推播調整與關閉 (對話一) (對話一)
    4. 第四階段：對話誤刪尋回與 LINE 方案評估 (對話二) (對話
    5. 第五階段：搜尋引擎升級與重疊搶答修復 (對話二) (對話
    6. 第六階段：讀取效能終極優化 (對話二)(對話
    7. 第七階段：目錄整併與啟動機制轉移 (對話三) (對話
    8. 第八階段：行動盤點助手開發與程式庫統一、槽位分離 (對話四) (對話
    9. 第段：智缺貨主要低進程推實(對話
    10. 第十階段：本機視覺化設定後台與多機器人 Slots 配置 (對話六) (對話
    11. 第十一階段：同音字保護傘、備份伺服器故障異體備份與對稱查詢 (對話七) (對話
    12. 第十二階段：API 金鑰安全鎖、盤點網站模糊查詢與設定後台功能擴充 (對話八) (對話

----------------------------------------------------------------------
一、 系統目前最終設定值 (config.json)
----------------------------------------------------------------------
檔位置：C:\agy_Add_on\ERP_System\config.json
設定內容：
{
  "databasePath": "\\\\192.168.1.99\\d\\nmedi\\stock.DBF",
  "lowStockThreshold": 5,
  "pushTimes": [
    "08:30",
    "17:00"
  ],
  "priorityProducts": [],
  "bots": [
    {
      "name": "BOT_1",
      "token": "<TELEGRAM_BOT_TOKEN_REMOVED>",
      "features": [
        "query",
        "history",
        "take",
        "push"
      ],
      "authorizedChats": [
        5976208790
      ]
    }
  ],
  "apiToken": "telcradrq0ps7q8yv5apyf"
}
* 說明：
  - `bots` 開機器人置底陣可以自訂人Token稱啟用查價 query歷售罄history盤點take庫存存推push）並管獨立 authorizedChats 選單
  - `apiToken` 系統自產執行外網 API 安全防範護

--------------------------------------------------
二、 系統架構與檔案清單
--------------------------------------------------
所有檔案已完全整理至以下本機路徑。為維護安全防護，本系統不在 Z 槽 (NAS) 進行 any 遠端程式備份。

主要本機執行路徑：C:\agy_Add_on\ERP_System\
1. telegram_bot.js (查價與歷史銷售及智慧推播機器人)
   負責 Telegram 查價與關鍵字客戶歷史成交價查詢與主動推播智慧警告與異常監控（背景執行）。
2. take_server.js (盤點與設定伺服器)。
   負責監聽本地 Port 3000 提供視覺化設定後台，並透過 Localtunnel 穿透至外網網址並主動推播異常與備份結果，帶有 API 金鑰保護機制。
3. config.json (統一設定檔)。
   支援多個機器人與智慧型警告、API 金鑰及資料庫設定。
4. stock_snapshot.json (庫存狀態快照檔)。
   智慧缺貨比對程式，用來記錄上次檢查之庫存數，防訊息重複發送。
5. append_take.ps1 (寫入腳本)。
   將盤點上傳之條碼、數量、主/次單位寫入，並非同步寫入 VFP 資料庫之 take.dbf 與 take1.dbf。
6. public/ (前端網頁資源資料夾)。
   提供條碼掃描行動端 UI，包含 index.html、index.css 與 index.js（含廠商模糊過濾選單）。
7. run_query.bat / run_query.vbs (查價背景啟動腳本)。
   用於手動/背景無字元視窗啟動 telegram_bot.js。
8. run_take.bat / run_take.vbs (盤點背景啟動腳本)。
   用於手動/背景無字元視窗啟動 take_server.js。
9. 自動開機啟動代理 (ERP_隨身助手.vbs)。
   置於啟動 Startup（開機自啟資料夾），開機自動執行並守護「查價」與「行動盤點網頁」服務。
10. run_bot.vbs (內容腳本)
    置於 C:\agy_Add_on\run_bot.vbs，防Windows 警報

----------------------------------------------------------------------
三歷史開發話
----------------------------------------------------------------------

第一段第七段】（詳見含資表位、交搜尋優、推、單一Port 28256 機制導入快功能，以開機自由移轉至 Startup 資夾）

【第八階段：行動盤點助手開發與程式庫統一、槽位分離】
* 用戶提出：
  - 開發前端盤點式選單不要強、顯示當庫存存
  - 在 Telegram 輸入「盤點」時，要求推播專屬網址。
  - 希望將程式獨立於查價資料夾，避免與查價機器人搞混。
  - 後續要求將盤點程式「寫在一起」以便維護，並「新增多個機器人插槽」以便決定是否啟用該機器人，且讓備份獨立。
  - 因 ERP 系統商在維護 NAS 共用區，要求本程式不得自 Z 槽載入程式碼，要求進行本地備份。
* 開發與優化：
  1. **行動端盤點網頁開發**：架設網頁伺服器（Port 3000）並提供條碼掃描與模糊過濾，實作本地防寫鎖，使用 PowerShell 將盤點資料 Append 入 VFP `take.dbf`（單頭）與 `take1.dbf`（明細）。
  2. **程式庫統一與多機器人插槽**：為了避免程式碼分散，將「行動盤點助手」與「ERP 隨身查」統一整併至同一路徑下，並更新 `ERP_System` 設定檔 `config.json` 結構，配置 `queryBot` 與 `takeBot` 多個插槽。當本地伺服器啟動時，自動建立 Localtunnel 穿透，將安全網址寫入 `active_url.txt`；查價 Bot 收到「盤點」命令時，自動讀取該檔案並回傳網址。
  3. **安全**：啟用戶要Z 槽中份夾完全刪除，保 C 槽，確保系統讀。更，確保其查價盤點點

第九段智慧「缺貨主要低進程推實作
* 用戶提出：
  - 將原先的一般低庫存推播程式停用，改用智慧型差額比對篩選，防止兩次重複推播造成洗版。
  - 含全部一在庫存好0 (貨)觸控發警報；只架設「熱、查價主要進異常低庫存水位推
* 開發實作
  1. **資診*：檢查測，ERP 中1,529 筆正常銷目庫存存為 0，篩選接推播 0 庫依然Telegram 訊息截
  2. **智慧缺貨偵測*
     - 在程式中引入 `stock_snapshot.json` 快照比對機制。每次推播時間（設定為 08:30 與 17:00），比對當前與前一次的庫存數。
     - **庫存存大於 0 變為小於等於 0（歸零零），觸控Telegram 推播警報**，其餘已0 庫複
  3. **主低庫存警*
     - 在 `config.json` 中新增 `priorityProducts` 欄位（主要商品編號陣列）。
     - 代理要主要商品庫存低於預設門檻，便會寫入警報。主要商品的進出庫變化將會影響其在有貨/缺貨的警報通知。
     - 此設計落實了「無事不擾」，精準防止缺貨漏單，精簡訊息。
  4. ****：在背景以新腳本程條碼 `telegram_bot.js` ，進程智慧測功能已

第段視覺架設定多人 Slots 置底
* 用戶提出：
  - 希建一覺人設後台
  - 欄含：BOT_1（可稱）、Token，以可選（查、盤點、庫存推，"BOT_1  宋為 TOKEN  查價(V)  (V)  庫推播(V)"
* 開發實作
  1. **視覺架設定*
     - 開發 `public/config.html`、`public/config.css` 與 `public/config.js` 設定後台。
     - 調整 UI 視覺佈局：包含資料庫路徑、庫存警報滑桿、定時推播時間的增加與減少。
     - 機器Slots 管：可/刪除機器人每台機器人顯示稱輸Bot Token，以核取方塊「查(Query)、「盤(Take)、「庫存推(Push) Chat ID
  2. **伺服器後端安全防護**：
     - 在 `take_server.js` 中新增 `/api/config` 讀寫設定之 API 路由，以支援前端後台的讀寫存取功能。
     - **實前端防範護*：檢`remoteIp` `127.0.0.1` `req.headers.host` 頭`localhost`/`127.0.0.1` 且無 any Localtunnel 轉發標頭 (x-forwarded-*)驗證透過外網 Localtunnel 保存架設面收到403 Forbidden，Token 洩安全全顧慮
  3. **多輪詢機器人實例管理**：
     - 重構 `telegram_bot.js` 結構，定義 `TelegramBotInstance` 類別，讓每台機器人各自維護其 updateOffset 與 Polling 輪詢實例。
     - 架設定 config.json 變更（透後台Telegram 註對話，系統file watch 機制測，並調（Reconcile）執行中Bot 實：新增 Polling，修被刪即時更止 Polling 放資
     - 自動庫存推播也已改為尋找設定檔中含有 `push` 功能 the 機器人，實施多通道警報發送。
  4. **測*
       - 已經成功部署並確認後台運作正常。經由外網 Localtunnel 訪問 `/config` 會收到 403 Forbidden 驗證攔截，本地防護極為穩固。湯

第段：保護、器異體對
* 用戶提出：
  - 華大」，即使輯，在 Telegram 上出搜尋況
  - 腦捷徑徑，Windows Script Host 「不足誤視
  - 希淋淋溼一字尾差濕導致系統跨況
  - 管主要無法在設後台利
* 開發與優化：
  1. **背景常進程步**
     經診「華大」搜尋對稱，是為執行輪詢人API 伺並仍在不式碼背景 Node.js 進安全全終止，並新WMI 模異常載入進程，已精準準查4 筆大
  2. **載入(VBScript) ASCII **
      `ERP_身vbs` 中帶中字路徑，在 Windows WSH 引下容開發編碼崩潰示不足演算法錯誤」 `ERP_身vbs` 00% 純英ASCII 腳本啟用 FSO `ERP` 頭夾路，並啟用 `cmd.exe /c run_query.bat` ，徹底根Windows 系統開機報並自執行（`telegram_bot.log` `take_server.log`）
  3. **資庫存防溢出記憶體**
     防範ERP 系統維護寫入 `stock.DBF` 不檔or 超長 RecordCount，在 `readStockDb` 中新增物長度防溢出 check。若現檔移超出 fileBuffer 度強跳迴確認進程任何極端況CPU 100% 假死OOM 記憶體崩潰
  4. **音字自(normalizeText)**
     搜尋對中導入 `normalizeText` 模，異常字簡稱字`溼` `濕``` ```綿` `棉``` `` 等對系統標。論使者輸種寫法可檢查索庫存存中任寫之。測試「淋溼淋精準準7 筆符
  5. **後台主入 Bug 修復 (Cache )**
     架設後台渲結`onclick` 事件接將述稱字串入 HTML，而醫稱中常常表示引（`"`， `3M 2"x3"`）導致 HTML 屬性構被斷，失。修為將搜尋保存在 JS `searchResultsCache` 記憶體中，純英數字編（NO）徹治此網演算法突並API 援 `nos` 次載入資

第段：API 鑰安全、盤點架設後台面功能
* 用戶提出：
  - 強外網安全防範護，為行外網 API Token 驗，防止外網惡庫存存廠資 API
  - 外網下選單中廠選單長，要支在上快模糊搜尋觸控控
  - 一：【客屬歷售罄查詢】。業在 Telegram 輸入客戶稱如`大白 佳診`）出該客近次購買該日、數交單
  - Telegram 中管後台localhost被官方安全機制，求可點選
  - 將新開發「歷售罄查詢」以形，新增至設後台「選」中
* 開發實作
  1. **外網 API 鑰安全*
     - 伺器`config.json` 中`apiToken` 自隨機22 位安全全
     - Telegram 送盤點網自在 URL 結尾帶 `token=telcradrq...`開機端網載入將該 token 存入 `sessionStorage`，並隨後 API 請中以 `x-api-token` Header 送
     - `take_server.js` 後台檢測如來自腳本機外網）強進 token 比，未請一律401 Unauthorized，徹底庫存存洩風
  2. **外網 UI 模糊*
     - 開機盤點public/index.html）廠下選單中整「模糊搜尋過濾器。使者在框輸注選單篩選符廠，並大點觸控控，解決開機操作
     - 修復 CSS card 容器下緣被， `overflow: hidden` 為 `visible`
     - 建一空代腳本`C:\agy_Add_on\run_bot.vbs` 以相容於 Windows 腳本消除檔案視窗
  3. **客戶專屬歷史查價查詢 ()**
     - `telegram_bot.js` 中新增關鍵字類器當到個以上關鍵詞，系統`CUST.DBF`為止混，關鍵字`75`）模糊匹為客戶稱
     - 實 1.3 GB 大 `ougo1.dbf` `queryDbfOptimized` 檔尾以 5,000 筆為讀緩衝到該客 3 筆該交紀就立刻傳，外網路啟用資夾查詢20 幾壓縮200 毫
  4. **Telegram 腳本 (lvh.me) 繞防範護**
     - 於 Telegram 官方封 `localhost` / `127.0.0.1` 作為超，架設後台為啟用外網 `http://lvh.me:3000/config`
     - 步新 `take_server.js` 信任本Host 規，新增 `lvh.me` 過濾放行性讓使者可接Telegram App 中腳本機設定
  5. **後台選」擴**
     - 腳本機設定網`public/config.html` & `config.js` 人置底 **歷售罄(History)* 
     - `config.css` 中為 `.bot-features` 添 `flex-wrap: wrap` 止後介面碎
     - 修改 `telegram_bot.js` 輯實現功能模流聯過濾：當查」啟用歷售罄」，查詢被妥絕歷史查價；當歷售罄」，歷查詢安全全（Fallback）為一模糊對確認主要制

第十三階段：搜尋強度升級、智慧型詞性定序、PowerShell 路徑模擬與設定後台升級 (對話九)
* 啟用戶架構審
  - 系統輸`3m1` `精準75` 等無空格混關鍵詞找不到，要空混並中專案
  - 對於選單位例寸/」、「尺/」，自準對，使者困
  - 歷史交查詢可「、「藥無客戶稱，要強單字防誤判通用詞診藥局等查詢輯
  - 寫入 PowerShell 腳本寫死Z 槽路徑庫存路變更且無崩潰且寫兩檔若中一檔被 ERP ，寫頭無明細毀
  - 後台鑰Math.random() 足且長輪詢推模組要強超以防 Socket 洩
  - 推播模依然叫已移盤點變數，可ReferenceError
  - 希將「管後台純字也改Telegram Inline Keyboard 互現
* 開發實作
  1. **智慧*
     -  `readStockDb` 。中關鍵詞`精準`）性混字串（ `3m1`）在數字中字智慧
     - 引入開機制：湊匹配100  AND  10 。搜尋得，確保精準確匹配永第一位
  2. **內容**
     - `normalizeText` 入 `.replace(/g, ').replace(/g, ')` ，選單位編碼起失
  3. **客戶歷史查價查詢誤**
     - 度1 中制客戶稱模糊匹配必精準確等於簡稱稱
     - 匹配到客大3 啟用詞，清空並自級為一庫存存查詢
  4. **PowerShell 資料庫路徑：檢防範護**
     - [take_server.js](file:///C:/agy_Add_on/ERP%20%E9%9A%A8%E8%BA%AB%E6%9F%A5%E8%88%87%E7%9B%A4%E9%BB%9E/take_server.js) 叫資料庫路徑：-DbDir 入 [append_take.ps1](file:///C:/agy_Add_on/ERP%20%E9%9A%A8%E8%BA%AB%E6%9F%A5%E8%88%87%E7%9B%A4%E9%BB%9E/append_take.ps1)
     - [append_take.ps1](file:///C:/agy_Add_on/ERP%20%E9%9A%A8%E8%BA%AB%E6%9F%A5%E8%88%87%E7%9B%A4%E9%BB%9E/append_take.ps1) 實檔檢查(Pre-lock Check)寫串串流，試 take.dbf take1.dbf若被，直警報退保護資一性
  5. **密碼學Token 超開機制**
     - API Token 使Node.js crypto.randomBytes(16).toString('hex') 安全
     - 封 Telegram https.request 並timeout 超，主調req.destroy() 堵劣網絡下洩
  6. **管關鍵ReferenceError 修正**
     - 修正 triggerLowStockPush 推播錯誤塊盤點考完全啟用 getBotKeyboard(bot) 
  7. **管後台口 Inline Keyboard *
     - 修改 `telegram_bot.js`   管後台 輯傳盤點此管後台現 UX 體
  8. **步 GitHub *
     - 步將本telegram_bot.jstake_server.js append_take.ps1 複製覆桌面乾淨倉庫並推至 GitHub腳本機全RUNNING

第十四階段：Localtunnel 假死自癒、雙向 AI 助理橋接器及免沙箱指令攔截 (對話十)
* 啟用戶架構審
  - 行外網內容到 Localtunnel 假死，外網被，系10-90自殺死循導致完全不可
  - AI 開機接對話前端截、日誌，IDE 沙箱審YES/NO 彈）阻礙
* 開發實作
  1. **Localtunnel 存活檢測假死自*：在 `take_server.js` 中90 Keep-Alive 存活檢查若超調`taskkill` 清樹並級為外網，100% 
  2. **噪持久 Token**：只次推播網，自律。修復 localStorage 被錯誤構污 401 
  3. **AI 橋接程 (telegram_bridge.js)**：建立器輪詢 `@agy_messenger_bot`，通話寫`telegram_inbox.txt` 中AI
  4. **審指派令*：在 `telegram_bridge.js` 中實``、「``、「``截，IDE 沙箱彈審核，直在背景安全

第十五階段：AI 助理專屬隔離、Windows 系統列托盤常駐監控與啟動整合 (對話十一)
* 啟用戶架構審
  - AI 式碼腳本機腳選單整至資夾不ERP 常查、盤點式混
  - 次都IDE 點」擾，供背景
  - 希AI 透過 Windows 面角工系統盤點）小示快並供、止、查外網網等選
  - 修復 WScript 對於中 `C:\agy_Add_on\ERP_System` 解碼錯誤以背景父視止命
* 開發實作
  1. **AI 專案屬隔離**：在主目建 `ai_assistant/`， `telegram_bridge.js`指派令腳`screenshot.ps1``restart_services.ps1` 等移入中，並將內對路徑構為路徑
  2. **Windows 系統監(tray.ps1)**：使.NET WinForms 術示左鍵）選單點選 AI 對話功能電 Telegram 對話；右選供查外網網止、退式功能，極完全額 npm 套件
  3. **整(VBS 移編碼修**
     - 徹 Windows Script Host (wscript) 對於BOM UTF-8 中路徑`` 導致退Bug， wrapper VBS (`run_take.vbs`, `run_query.vbs`, `run_bridge.vbs`) 新CP950 (Big5) 編碼寫入並全移根純英路徑`C:\agy_Add_on\` 底，內絕路徑引
     - 寫 `restart_services.ps1`： node 啟用 `WScript.Shell` 叫純英路徑徑VBS WrapperVBS 中 WScript.Shell.Run 完全隔離（Detached）命性讓這Node 背景PowerShell/cscript 腳本後依然穩獨立常駐不被 Sandbox 安全收到
  4. **IDE 權*：主 IDE 了 `Desktop\vfp-erp-telegram-agent` `git` 指派令永久核徹消了日常步GitHub  IDE 確彈
  5. **件更*：更 `使用說.txt` `對話紀txt`，並步送至 GitHub，構工作

第十六階段：發想計畫書整理、即時售罄告警與欠貨後送系統整合 (對話十二)
* 啟用戶架構審
  - 專計畫份獨立想計畫自焦系統架設計思維
  - 每份想計畫補詳細系統論(Discussion Points) 以供未來優
  - 將檔案路徑 `ERP 身` 修正 `ERP_System`
  - 清專案，移、複討臨開發件
* 開發與優化：
  - **計畫份獨立詳細*
        1. `【已上線】發想計畫書_隨身查系統.txt`：專為業務員在查價與銷貨歷史逆向檢索技術。針對中斷資料庫逆向重建與追溯機制討論。
        2. `【已上線】發想計畫書_即時售罄告警系統.txt`：新型態寫入於每日庫存輪詢、廠商模糊過濾、Inline Callback 查詢等即時售罄推播技術。優化營業節奏與告警機制討論。
        3. `【已上線】發想計畫書_欠貨後送紀錄系統.txt`：新型態寫入於 VFP 批次出貨單自動對帳、雲端 GAS Webhook 主動發送、Unique-Key 註冊與併單防重送之欠貨後送管理技術。非同步衝突防範與業務權限管理討論要點。
* 說明：
  - `bots` 開機器人置底陣可以自訂人Token稱啟用查價 query歷售罄history盤點take庫存存推push）並管獨立 authorizedChats 選單
  - `apiToken` 系統自產執行外網 API 安全防範護

--------------------------------------------------
二、 系統架構與檔案清單
--------------------------------------------------
所有檔案已完全整理至以下本機路徑。為維護安全防護，本系統不在 Z 槽 (NAS) 進行 any 遠端程式備份。

主要本機執行路徑：C:\agy_Add_on\ERP_System\
1. telegram_bot.js (查價與歷史銷售及智慧推播機器人)
   負責 Telegram 查價與關鍵字客戶歷史成交價查詢與主動推播智慧警告與異常監控（背景執行）。
2. take_server.js (盤點與設定伺服器)。
   負責監聽本地 Port 3000 提供視覺化設定後台，並透過 Localtunnel 穿透至外網網址並主動推播異常與備份結果，帶有 API 金鑰保護機制。
3. config.json (統一設定檔)。
   支援多個機器人與智慧型警告、API 金鑰及資料庫設定。
4. stock_snapshot.json (庫存狀態快照檔)。
   智慧缺貨比對程式，用來記錄上次檢查之庫存數，防訊息重複發送。
5. append_take.ps1 (寫入腳本)。
   將盤點上傳之條碼、數量、主/次單位寫入，並非同步寫入 VFP 資料庫之 take.dbf 與 take1.dbf。
6. public/ (前端網頁資源資料夾)。
   提供條碼掃描行動端 UI，包含 index.html、index.css 與 index.js（含廠商模糊過濾選單）。
7. run_query.bat / run_query.vbs (查價背景啟動腳本)。
   用於手動/背景無字元視窗啟動 telegram_bot.js。
8. run_take.bat / run_take.vbs (盤點背景啟動腳本)。
   用於手動/背景無字元視窗啟動 take_server.js。
9. 自動開機啟動代理 (ERP_隨身助手.vbs)。
   置於啟動 Startup（開機自啟資料夾），開機自動執行並守護「查價」與「行動盤點網頁」服務。
10. run_bot.vbs (內容腳本)
    置於 C:\agy_Add_on\run_bot.vbs，防Windows 警報

----------------------------------------------------------------------
三歷史開發話
----------------------------------------------------------------------

第一段第七段】（詳見含資表位、交搜尋優、推、單一Port 28256 機制導入快功能，以開機自由移轉至 Startup 資夾）

【第八階段：行動盤點助手開發與程式庫統一、槽位分離】
* 用戶提出：
  - 開發前端盤點式選單不要強、顯示當庫存存
  - 在 Telegram 輸入「盤點」時，要求推播專屬網址。
  - 希望將程式獨立於查價資料夾，避免與查價機器人搞混。
  - 後續要求將盤點程式「寫在一起」以便維護，並「新增多個機器人插槽」以便決定是否啟用該機器人，且讓備份獨立。
  - 因 ERP 系統商在維護 NAS 共用區，要求本程式不得自 Z 槽載入程式碼，要求進行本地備份。
* 開發與優化：
  1. **行動端盤點網頁開發**：架設網頁伺服器（Port 3000）並提供條碼掃描與模糊過濾，實作本地防寫鎖，使用 PowerShell 將盤點資料 Append 入 VFP `take.dbf`（單頭）與 `take1.dbf`（明細）。
  2. **程式庫統一與多機器人插槽**：為了避免程式碼分散，將「行動盤點助手」與「ERP 隨身查」統一整併至同一路徑下，並更新 `ERP_System` 設定檔 `config.json` 結構，配置 `queryBot` 與 `takeBot` 多個插槽。當本地伺服器啟動時，自動建立 Localtunnel 穿透，將安全網址寫入 `active_url.txt`；查價 Bot 收到「盤點」命令時，自動讀取該檔案並回傳網址。
  3. **安全**：啟用戶要Z 槽中份夾完全刪除，保 C 槽，確保系統讀。更，確保其查價盤點點

第九段智慧「缺貨主要低進程推實作
* 用戶提出：
  - 將原先的一般低庫存推播程式停用，改用智慧型差額比對篩選，防止兩次重複推播造成洗版。
  - 含全部一在庫存好0 (貨)觸控發警報；只架設「熱、查價主要進異常低庫存水位推
* 開發實作
  1. **資診*：檢查測，ERP 中1,529 筆正常銷目庫存存為 0，篩選接推播 0 庫依然Telegram 訊息截
  2. **智慧缺貨偵測*
     - 在程式中引入 `stock_snapshot.json` 快照比對機制。每次推播時間（設定為 08:30 與 17:00），比對當前與前一次的庫存數。
     - **庫存存大於 0 變為小於等於 0（歸零零），觸控Telegram 推播警報**，其餘已0 庫複
  3. **主低庫存警*
     - 在 `config.json` 中新增 `priorityProducts` 欄位（主要商品編號陣列）。
     - 代理要主要商品庫存低於預設門檻，便會寫入警報。主要商品的進出庫變化將會影響其在有貨/缺貨的警報通知。
     - 此設計落實了「無事不擾」，精準防止缺貨漏單，精簡訊息。
  4. ****：在背景以新腳本程條碼 `telegram_bot.js` ，進程智慧測功能已

第段視覺架設定多人 Slots 置底
* 用戶提出：
  - 希建一覺人設後台
  - 欄含：BOT_1（可稱）、Token，以可選（查、盤點、庫存推，"BOT_1  宋為 TOKEN  查價(V)  (V)  庫推播(V)"
* 開發實作
  1. **視覺架設定*
     - 開發 `public/config.html`、`public/config.css` 與 `public/config.js` 設定後台。
     - 調整 UI 視覺佈局：包含資料庫路徑、庫存警報滑桿、定時推播時間的增加與減少。
     - 機器Slots 管：可/刪除機器人每台機器人顯示稱輸Bot Token，以核取方塊「查(Query)、「盤(Take)、「庫存推(Push) Chat ID
  2. **伺服器後端安全防護**：
     - 在 `take_server.js` 中新增 `/api/config` 讀寫設定之 API 路由，以支援前端後台的讀寫存取功能。
     - **實前端防範護*：檢`remoteIp` `127.0.0.1` `req.headers.host` 頭`localhost`/`127.0.0.1` 且無 any Localtunnel 轉發標頭 (x-forwarded-*)驗證透過外網 Localtunnel 保存架設面收到403 Forbidden，Token 洩安全全顧慮
  3. **多輪詢機器人實例管理**：
     - 重構 `telegram_bot.js` 結構，定義 `TelegramBotInstance` 類別，讓每台機器人各自維護其 updateOffset 與 Polling 輪詢實例。
     - 架設定 config.json 變更（透後台Telegram 註對話，系統file watch 機制測，並調（Reconcile）執行中Bot 實：新增 Polling，修被刪即時更止 Polling 放資
     - 自動庫存推播也已改為尋找設定檔中含有 `push` 功能 the 機器人，實施多通道警報發送。
  4. **測*
       - 已經成功部署並確認後台運作正常。經由外網 Localtunnel 訪問 `/config` 會收到 403 Forbidden 驗證攔截，本地防護極為穩固。湯

第段：保護、器異體對
* 用戶提出：
  - 華大」，即使輯，在 Telegram 上出搜尋況
  - 腦捷徑徑，Windows Script Host 「不足誤視
  - 希淋淋溼一字尾差濕導致系統跨況
  - 管主要無法在設後台利
* 開發與優化：
  1. **背景常進程步**
     經診「華大」搜尋對稱，是為執行輪詢人API 伺並仍在不式碼背景 Node.js 進安全全終止，並新WMI 模異常載入進程，已精準準查4 筆大
  2. **載入(VBScript) ASCII **
      `ERP_身vbs` 中帶中字路徑，在 Windows WSH 引下容開發編碼崩潰示不足演算法錯誤」 `ERP_身vbs` 00% 純英ASCII 腳本啟用 FSO `ERP` 頭夾路，並啟用 `cmd.exe /c run_query.bat` ，徹底根Windows 系統開機報並自執行（`telegram_bot.log` `take_server.log`）
  3. **資庫存防溢出記憶體**
     防範ERP 系統維護寫入 `stock.DBF` 不檔or 超長 RecordCount，在 `readStockDb` 中新增物長度防溢出 check。若現檔移超出 fileBuffer 度強跳迴確認進程任何極端況CPU 100% 假死OOM 記憶體崩潰
  4. **音字自(normalizeText)**
     搜尋對中導入 `normalizeText` 模，異常字簡稱字`溼` `濕``` ```綿` `棉``` `` 等對系統標。論使者輸種寫法可檢查索庫存存中任寫之。測試「淋溼淋精準準7 筆符
  5. **後台主入 Bug 修復 (Cache )**
     架設後台渲結`onclick` 事件接將述稱字串入 HTML，而醫稱中常常表示引（`"`， `3M 2"x3"`）導致 HTML 屬性構被斷，失。修為將搜尋保存在 JS `searchResultsCache` 記憶體中，純英數字編（NO）徹治此網演算法突並API 援 `nos` 次載入資

第段：API 鑰安全、盤點架設後台面功能
* 用戶提出：
  - 強外網安全防範護，為行外網 API Token 驗，防止外網惡庫存存廠資 API
  - 外網下選單中廠選單長，要支在上快模糊搜尋觸控控
  - 一：【客屬歷售罄查詢】。業在 Telegram 輸入客戶稱如`大白 佳診`）出該客近次購買該日、數交單
  - Telegram 中管後台localhost被官方安全機制，求可點選
  - 將新開發「歷售罄查詢」以形，新增至設後台「選」中
* 開發實作
  1. **外網 API 鑰安全*
     - 伺器`config.json` 中`apiToken` 自隨機22 位安全全
     - Telegram 送盤點網自在 URL 結尾帶 `token=telcradrq...`開機端網載入將該 token 存入 `sessionStorage`，並隨後 API 請中以 `x-api-token` Header 送
     - `take_server.js` 後台檢測如來自腳本機外網）強進 token 比，未請一律401 Unauthorized，徹底庫存存洩風
  2. **外網 UI 模糊*
     - 開機盤點public/index.html）廠下選單中整「模糊搜尋過濾器。使者在框輸注選單篩選符廠，並大點觸控控，解決開機操作
     - 修復 CSS card 容器下緣被， `overflow: hidden` 為 `visible`
     - 建一空代腳本`C:\agy_Add_on\run_bot.vbs` 以相容於 Windows 腳本消除檔案視窗
  3. **客戶專屬歷史查價查詢 ()**
     - `telegram_bot.js` 中新增關鍵字類器當到個以上關鍵詞，系統`CUST.DBF`為止混，關鍵字`75`）模糊匹為客戶稱
     - 實 1.3 GB 大 `ougo1.dbf` `queryDbfOptimized` 檔尾以 5,000 筆為讀緩衝到該客 3 筆該交紀就立刻傳，外網路啟用資夾查詢20 幾壓縮200 毫
  4. **Telegram 腳本 (lvh.me) 繞防範護**
     - 於 Telegram 官方封 `localhost` / `127.0.0.1` 作為超，架設後台為啟用外網 `http://lvh.me:3000/config`
     - 步新 `take_server.js` 信任本Host 規，新增 `lvh.me` 過濾放行性讓使者可接Telegram App 中腳本機設定
  5. **後台選」擴**
     - 腳本機設定網`public/config.html` & `config.js` 人置底 **歷售罄(History)* 
     - `config.css` 中為 `.bot-features` 添 `flex-wrap: wrap` 止後介面碎
     - 修改 `telegram_bot.js` 輯實現功能模流聯過濾：當查」啟用歷售罄」，查詢被妥絕歷史查價；當歷售罄」，歷查詢安全全（Fallback）為一模糊對確認主要制

第十三階段：搜尋強度升級、智慧型詞性定序、PowerShell 路徑模擬與設定後台升級 (對話九)
* 啟用戶架構審
  - 系統輸`3m1` `精準75` 等無空格混關鍵詞找不到，要空混並中專案
  - 對於選單位例寸/」、「尺/」，自準對，使者困
  - 歷史交查詢可「、「藥無客戶稱，要強單字防誤判通用詞診藥局等查詢輯
  - 寫入 PowerShell 腳本寫死Z 槽路徑庫存路變更且無崩潰且寫兩檔若中一檔被 ERP ，寫頭無明細毀
  - 後台鑰Math.random() 足且長輪詢推模組要強超以防 Socket 洩
  - 推播模依然叫已移盤點變數，可ReferenceError
  - 希將「管後台純字也改Telegram Inline Keyboard 互現
* 開發實作
  1. **智慧*
     -  `readStockDb` 。中關鍵詞`精準`）性混字串（ `3m1`）在數字中字智慧
     - 引入開機制：湊匹配100  AND  10 。搜尋得，確保精準確匹配永第一位
  2. **內容**
     - `normalizeText` 入 `.replace(/g, ').replace(/g, ')` ，選單位編碼起失
  3. **客戶歷史查價查詢誤**
     - 度1 中制客戶稱模糊匹配必精準確等於簡稱稱
     - 匹配到客大3 啟用詞，清空並自級為一庫存存查詢
  4. **PowerShell 資料庫路徑：檢防範護**
     - [take_server.js](file:///C:/agy_Add_on/ERP%20%E9%9A%A8%E8%BA%AB%E6%9F%A5%E8%88%87%E7%9B%A4%E9%BB%9E/take_server.js) 叫資料庫路徑：-DbDir 入 [append_take.ps1](file:///C:/agy_Add_on/ERP%20%E9%9A%A8%E8%BA%AB%E6%9F%A5%E8%88%87%E7%9B%A4%E9%BB%9E/append_take.ps1)
     - [append_take.ps1](file:///C:/agy_Add_on/ERP%20%E9%9A%A8%E8%BA%AB%E6%9F%A5%E8%88%87%E7%9B%A4%E9%BB%9E/append_take.ps1) 實檔檢查(Pre-lock Check)寫串串流，試 take.dbf take1.dbf若被，直警報退保護資一性
  5. **密碼學Token 超開機制**
     - API Token 使Node.js crypto.randomBytes(16).toString('hex') 安全
     - 封 Telegram https.request 並timeout 超，主調req.destroy() 堵劣網絡下洩
  6. **管關鍵ReferenceError 修正**
     - 修正 triggerLowStockPush 推播錯誤塊盤點考完全啟用 getBotKeyboard(bot) 
  7. **管後台口 Inline Keyboard *
     - 修改 `telegram_bot.js`   管後台 輯傳盤點此管後台現 UX 體
  8. **步 GitHub *
     - 步將本telegram_bot.jstake_server.js append_take.ps1 複製覆桌面乾淨倉庫並推至 GitHub腳本機全RUNNING

第十四階段：Localtunnel 假死自癒、雙向 AI 助理橋接器及免沙箱指令攔截 (對話十)
* 啟用戶架構審
  - 行外網內容到 Localtunnel 假死，外網被，系10-90自殺死循導致完全不可
  - AI 開機接對話前端截、日誌，IDE 沙箱審YES/NO 彈）阻礙
* 開發實作
  1. **Localtunnel 存活檢測假死自*：在 `take_server.js` 中90 Keep-Alive 存活檢查若超調`taskkill` 清樹並級為外網，100% 
  2. **噪持久 Token**：只次推播網，自律。修復 localStorage 被錯誤構污 401 
  3. **AI 橋接程 (telegram_bridge.js)**：建立器輪詢 `@agy_messenger_bot`，通話寫`telegram_inbox.txt` 中AI
  4. **審指派令*：在 `telegram_bridge.js` 中實``、「``、「``截，IDE 沙箱彈審核，直在背景安全

第十五階段：AI 助理專屬隔離、Windows 系統列托盤常駐監控與啟動整合 (對話十一)
* 啟用戶架構審
  - AI 式碼腳本機腳選單整至資夾不ERP 常查、盤點式混
  - 次都IDE 點」擾，供背景
  - 希AI 透過 Windows 面角工系統盤點）小示快並供、止、查外網網等選
  - 修復 WScript 對於中 `C:\agy_Add_on\ERP_System` 解碼錯誤以背景父視止命
* 開發實作
  1. **AI 專案屬隔離**：在主目建 `ai_assistant/`， `telegram_bridge.js`指派令腳`screenshot.ps1``restart_services.ps1` 等移入中，並將內對路徑構為路徑
  2. **Windows 系統監(tray.ps1)**：使.NET WinForms 術示左鍵）選單點選 AI 對話功能電 Telegram 對話；右選供查外網網止、退式功能，極完全額 npm 套件
  3. **整(VBS 移編碼修**
     - 徹 Windows Script Host (wscript) 對於BOM UTF-8 中路徑`` 導致退Bug， wrapper VBS (`run_take.vbs`, `run_query.vbs`, `run_bridge.vbs`) 新CP950 (Big5) 編碼寫入並全移根純英路徑`C:\agy_Add_on\` 底，內絕路徑引
     - 寫 `restart_services.ps1`： node 啟用 `WScript.Shell` 叫純英路徑徑VBS WrapperVBS 中 WScript.Shell.Run 完全隔離（Detached）命性讓這Node 背景PowerShell/cscript 腳本後依然穩獨立常駐不被 Sandbox 安全收到
  4. **IDE 權*：主 IDE 了 `Desktop\vfp-erp-telegram-agent` `git` 指派令永久核徹消了日常步GitHub  IDE 確彈
  5. **件更*：更 `使用說.txt` `對話紀txt`，並步送至 GitHub，構工作

第十六階段：發想計畫書整理、即時售罄告警與欠貨後送系統整合 (對話十二)
* 啟用戶架構審
  - 專計畫份獨立想計畫自焦系統架設計思維
  - 每份想計畫補詳細系統論(Discussion Points) 以供未來優
  - 將檔案路徑 `ERP 身` 修正 `ERP_System`
  - 清專案，移、複討臨開發件
* 開發與優化：
  - **計畫份獨立詳細*
        1. `【已上線】發想計畫書_隨身查系統.txt`：專為業務員在查價與銷貨歷史逆向檢索技術。針對中斷資料庫逆向重建與追溯機制討論。
        2. `【已上線】發想計畫書_即時售罄告警系統.txt`：新型態寫入於每日庫存輪詢、廠商模糊過濾、Inline Callback 查詢等即時售罄推播技術。優化營業節奏與告警機制討論。
        3. `【已上線】發想計畫書_欠貨後送紀錄系統.txt`：新型態寫入於 VFP 批次出貨單自動對帳、雲端 GAS Webhook 主動發送、Unique-Key 註冊與併單防重送之欠貨後送管理技術。非同步衝突防範與業務權限管理討論要點。
  - **格式*：採精準 ASCII 表TOC 等飾面前端前端閱讀體
  - **檔清**：以 `git rm` 徹移除了已啟用 `身系統論txt``後送系統論txt``已上開發書_欠貨後送系txt` 等檔保專案清爽
  - **使用說*：全說件話中檔案路徑糾 `C:\agy_Add_on\ERP_System`
  - **Git 庫存*： Commit Push，步至 GitHub 前端

第十七階段：AI 助理安全清退與智慧 ERP V8 雙向開發藍圖共識 (對話十三)* 用戶提出：  - 徹移除卸載載 AI （AGY MESSENGERai_assistant telegram_bridge.jsWMI 腳本檔案格審查，避其他核心影響響  - 修改 Telegram 查價機器人比：庫存上方無庫中已售罄/啟用律置底佔用面，幫仁第一警報* 開發實作
  1. **AI 安全全清退**     - 清除 config.json 中 aiAssistantToken 設     - 修改 scripts/restart_services.ps1，刪 telegram_bridge.js 進清 and VBS 指派令     - 刪除 C:\agy_Add_on\ai_assistant\ 專屬下盤點截指派令碼，並刪除 scripts 下 run_bridge.bat / run_bridge.vbs 殘     - 外，Port 28256 Port 3000 清爽，無多  2. **層置底**     - 修改 engine/vfp_net_driver.db readStockDb 演演算法     - 實施層層優序」庫存存（ 庫存存（中  標售罄/啟用置底）。層內評，美解決模糊查詢庫存/產干擾  3. **件*     - 清並更使用說.txt，拿AI 操作紹     - 將透過 Git 交並推到 GitHub 前端庫存  4. **外網 Gemini 智慧 ERP V8 開發**     - **D 槽大核心戰詳細輯隔離**       * D:\00_私人專  絕置底私防範護河。專案屬 VIP 私人空，任何式、自監份絕禁止跨此確認私人檔極致安全全       * D:\01_份  系統唯讀快隔離峰自步天外100%  File Lock       * D:\02_AI_測試沙  開發錯實室。中測試專屬，亦未來「IDE 開發介面建置本地固定Port 3001，自監主要跳       * D:\03_AI_上  產線。固定Port 3000，自監24 小守護       * D:\04_PostgreSQL  系統核心大。承載夜步乾淨歷史，（帳齡、年底報表     - **艦級視 IDE 開發建置 (PoC 概念驗**：於 D:\02_AI_測試沙 規左側 Gemini 輔助右Antigravity Agent IDE 稱。此將在沙獨立執行Port 3001）高開發刪除驗且不干正式環境     - **份鮮度檢(Freshness Check)**：白天唯讀份進程檢查測檔修改若超 24 小更 NAS 步進程卡死，系統自推警給 Bot 1 (主管/)，防範風     - **前端算資產活**：當ERP 完全體在 NAS 上並系統脫，迷你主機外轉，全為 AI 開發沙裝數中心前端穿 Tailscale 跳板     - **業客戶權綁**：Bot 2/3 (外勤業) 功能查詢派給自身責任客歷交價客保護客戶搶     - **級發票尾差防禦*：優VFP  TOT() TAX(稅) 欄；當 VFP 實收總論總額相減尾差±1，層自寫tax_adjustment 補以維帳借貸平衡；差額若大於 2 平帳並觸Excel 紅色警報，以式Bug     - **點陣表(Epson LQ-690C) *：前端直9.5 x 5.5 PDF 絕 (684 x 396 pt)，瀏覽直後台置底 X/Y 軸毫米置底 (Offset) 設滑桿，容許計仁自行微調物走紙公差
第十八階段：正式區全面穩定移轉、小工具 ERP 備份功能移轉與桌面捷徑清理 (對話十四)* 啟用戶架構審
  - 將「執ERP 份資（份指派令批次）移轉至專屬資`D:\00_私人專\01_外工具`，更日誌寫至該目，並修正次檔以路徑叫份腳本，避硬編碼CP950 編碼誤  - 新外工具（工具控面份呼路徑徑為腳本機目 `backup_daily.ps1`，避寫死正式線路徑  - 將「執行ERP份.lnk捷徑徑移保桌面乾淨* 開發實作
  1. **穩移*     - 已背景相捷徑徑資正式 `D:\03_AI_上`，並將且餘 `agyaddon` 主夾安全刪除，徹底空多檔  2. **Chrome 前端**     -  Chrome 架設定（DefaultProfile 1System Profile）記憶體模式     - `remotedesktop.google.com` Chrome 記憶體白選單     - Local State 中 `background_mode.enabled` 設為 `true`，以確 Chrome 繼續執行前端，解決前端異常性斷線盤點  3. **外網表 (EPSON LQ-690C) 安**     -  `192.168.1.98`通`9100` TCP/IP 並裝表     -  Z 槽 `LQ-690C_Drv_Ver4.zip`，隨後透管驅註腳本Driver Store Print Spooler，`"EPSON LQ-690C ESC/P2"` 驅進程  4. **執行ERP份移轉*     - **份腳本批次移**： `backup_daily.ps1` `run_manual_backup.bat` `D:\03_AI_上\scripts\` 移**`D:\00_私人專\01_外工具`** 本地資夾使份檔小工放一目     - **修正日誌路*       * 修改 `backup_daily.ps1` 將日誌路徑更為本地`.\logs\backup_daily.log`       * 修改 `run_manual_backup.bat` 使用 `%~dp0` PowerShell 轉義引，以路徑叫本地`backup_daily.ps1`，美中路徑徑空白       * 新 `service_monitor_widget.ps1` `service_monitor_gui.ps1` 「份」件為步呼腳本 `"$scriptDir\backup_daily.ps1"`，寫死任絕路徑     - **桌面份捷徑*：使者求已桌面`立即ERP份.lnk` 安全全移除，整潔使用可接從工具上份

第十九階段：銷貨歷史查詢效能與即時反饋優化 (對話十五)
* 用戶提出：
  - 改善 1.34 GB 銷貨歷史檔 (ougo1.dbf) 的查詢效能，避免大檔查詢過慢及 NAS 資料庫鎖定。
  - 當查詢正在處理中時，Telegram Bot 應顯示「正在輸入...」的狀態（typing），並在點擊「歷史價」快捷按鈕時顯示即時進度提示，提供良好體感並防範重複點擊。
  - 沙盒環境與正式環境共用 Bot Token 造成 Telegram 409 連線衝突，需要排除。
* 開發與優化：
  - **修復語法錯誤**：修正了 `vfp_net_driver.db` 中 `checkProductKeywordsExist` 遺漏的關閉括弧，解決 Node.js 解析崩潰語法問題。
  - **銷貨歷史索引查詢**：在記憶體中建立銷貨歷史 CUST/OUGO1 的客戶案號（`NO`）索引地圖，快速定位特定客戶的歷史成交明細，將大檔查詢效能從數秒縮短至 1 毫秒級別，實現數千倍提速，且零鎖定、不增加 NAS I/O 負擔。
  - **UX 即時反饋機制**：
    - 擴充 `answerCallbackQuery` 通訊模組，支援可選參數 `text`，點擊按鈕時即刻回傳「🔍 正在查詢歷史成交價...」提示氣泡。
    - 實作 `sendTelegramAction` 函數，在歷史成交查詢執行期間啟動 Telegram 的 `typing` 輸入狀態。
  - **防 409 連線衝突**：關閉並停止沙盒背景 Bot 輪詢服務，僅保留正式環境單一 Bot 輪詢，徹底消除 Telegram 連線衝突，使 Bot 回應流暢且穩定。
  - **部署與重啟**：將優化代碼同步至正式區的 `D:\03_AI_正式上線\engine\vfp_net_driver.db` 並重啟正式背景服務。

